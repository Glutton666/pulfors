// Mode tutorials are temporarily disabled while their target positioning is
// being revisited. Keep the implementation in place so it can be re-enabled
// without rebuilding the flow.
export const MODE_TUTORIALS_ENABLED = false;