🎧 사용자(User) 관점
좋은 점: 9단계 온보딩(테마·사운드·햅틱/플래시·음성명령 미리보기 포함), 커스텀 훅으로 로직 분리, 다국어 지원.

아쉬운 점:

설정 화면(SettingsModal)이 오디오/시각/동작 설정이 섹션 구분 없이 나열돼 있어 원하는 항목을 찾기 어려움
접근성 라벨이 전체 코드 8만 줄 중 약 265개뿐 — 스크린리더 사용자는 재생/정지, BPM 슬라이더 등 핵심 컨트롤의 용도를 알기 어려움
터치 영역이 40×36px 등 iOS 권장 최소(44×44)보다 작은 곳 다수
모달이 13개 이상 하나의 activeModal 상태머신으로 관리되어, 새 모달 추가 시 버그 유발 가능성 높음
🎼 음악가(Musician) 관점
좋은 점: 튜닝 데이터가 A440·평균율 기준으로 정확, BPM 감지(온셋 검출+자기상관)가 업계 표준 방식, 악보 렌더러가 클레프·조표·타이/도트 리듬을 정확히 처리, 연습실(Practice Room) 기능이 실질적 연습 습관 추적에 도움됨.

아쉬운 점:

복합박자(6/8, 9/8, 12/8) 미지원: 분자/분모만으로 처리해서 6/8을 "6개의 단순 박"으로 잘못 표시할 수 있음 — 음악적으로 꽤 중요한 결함
셋잇단음(triplet) 노트값이 악보 에디터에 없음 — 재즈/스윙, 클래식 트리플렛 표기 불가
BPM 감지 범위가 50–250으로 하드코딩되어 미니멀/앰비언트(느림)나 스래시메탈(매우 빠름) 등 극단적 템포는 수동 입력 필요
평균율만 지원, 고전 조율법 등은 없음 (마이너 이슈)
🎹 연주자(Performer) 관점
좋은 점: iOS/Android 백그라운드·잠금화면 재생, 오디오 포커스(전화 인터럽트 등) 처리가 상당히 견고함 — 무음 프로브로 포커스 손실 감지 후 자동 일시정지, 포그라운드 서비스로 백그라운드 재생 유지.

아쉬운 점:

박자 표시(비트 인디케이터)가 원형 다이얼 색상/테두리 변화 위주라, 연주 중 흘낏 보는 상황에서 "지금 몇 박째인지"를 즉각 파악하기 어려울 수 있음 → 중앙에 큰 숫자 표시나 더 강한 플래시 대비 권장
Android 오디오 포커스 프로브가 300ms 간격 폴링이라, 아주 짧은 인터럽트(300ms 미만)는 놓쳐서 메트로놈이 계속 멈춰있을 수 있는 레이스 컨디션 존재
BarModeView 등 정보 밀도가 높아 무대 위 빠른 손가락 조작(원핸드) 시 오터치 위험
⚙️ 엔지니어(Engineer) 관점
좋은 점: "Tale of Two Clocks" 패턴 적용 — measureStartTime을 앵커 기반으로 재계산해 부동소수점 드리프트 누적을 방지(lib/metronome-engine.ts), lookahead 스케줄링으로 JS 스레드 지연에도 끊김 방지, 오디오 플레이어 30개 사전 로드로 재생 지연 최소화, 스케줄 캐시(fingerprint 기반)로 재계산 비용 절감. 100마디 드리프트 제로 테스트까지 존재.

아쉬운 점:

타이밍이 여전히 JS 기반 스케줄링(performance.now() 비교)이라, 네이티브 오디오 클록과 직접 동기화하진 않음 — 캐주얼 연습용으로는 충분하지만 스튜디오/샘플 정확도가 필요한 용도엔 부족
Android 포커스 프로브·알림 리스너 정리(cleanup)에 레이스 컨디션/리소스 누수 가능성 (android-audio-focus.ts, notification-controls.ts)
커스텀 샘플을 렌더링 시점에 디코딩하는 경로가 있어, 샘플을 빠르게 교체하면 재생 끊김 가능성
💻 개발자(Developer) 관점
좋은 점: 백업/복구가 트랜잭션 기반 스냅샷+자동 롤백으로 매우 견고, 에러 트래킹(Sentry 연동)과 ErrorBoundary가 프로덕션급, 디바운스+재시도 백오프를 갖춘 영속성 레이어, 타입 강제되는 i18n 구조, 서버의 레이트리미팅·요청 크기 제한 등 보안 고려, 테스트가 타이밍/보안/백업 등 폭넓고 실질적.

아쉬운 점:

메인 컴포넌트 비대화: app/index.tsx가 5,800줄, useState 60개 이상 — 유지보수·리렌더 성능 리스크
오토코릴레이트(자기상관) 알고리즘이 서버 코드에 3곳 중복 구현 — 버그 수정 시 3곳 다 고쳐야 함
서버 백엔드가 미완성: IStorage/Drizzle 스키마가 정의만 되고 실제로 쓰이지 않는 죽은 코드로 보임 — 랜딩페이지 용도인지 명확히 문서화 필요
백업 복구 시 Record<string, unknown> 캐스팅 위주라 타입 안전성이 약함 — Zod 등으로 스키마 검증 강화 권장