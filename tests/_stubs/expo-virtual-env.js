// Stub for expo/virtual/env.js — created by babel-preset-expo when
// EXPO_PUBLIC_* env vars are referenced. Jest can't load the real
// ESM module from node_modules, so we return a plain CJS version.
const env = process.env;
module.exports = { env };
module.exports.default = { env };
